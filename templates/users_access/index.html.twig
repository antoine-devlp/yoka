<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="https://bootswatch.com/5/minty/bootstrap.min.css">
        <title>{% block title %}Bienvenue sur Yoka !{% endblock %}</title>
    </head>
    <body>

        {% block body %}
        {% if not app.user %}
        <div class="p-5">
            <h1 class="text-center mb-4">Bienvenue sur Yoka !</h1>
            <p class="text-center"> Grâce à Yoka, liste et suis les séries et films que tu regardes !</p>
            <div class="d-grid gap-2">
                <a href="{{path('users_register')}}" class="text-center"><button class="btn btn-lg btn-primary" type="button">Inscription</button></a>
                <p class="text-center mt-4"> Vous avez déjà un compte ? </p>
                <a href="{{path('users_login')}}" class="text-center"><button class="btn btn-lg btn-info" type="button">Connexion</button></a>

            </div>
        <div>
        {% else %}
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">Yoka</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                     <span class="navbar-toggler-icon"></span>
                </button>

                 <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="#">Acceuil
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="">Mes series</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="">Mes films</a>
                        </li>
                    </ul>
                 </div>
            </div>
        </nav>
           <a href="{{path('users_logout')}}" class="text-center"><button class="btn btn-lg btn-info" type="button">Deconnexion</button></a>
        {% endif %}
        {% endblock %}

      
        
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    </body>

</html>