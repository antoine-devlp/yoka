{% extends 'base.html.twig' %}

{% block title %}Films{% endblock %}

{% block body %}
    <h1>Films</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ film.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ film.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('film') }}">back to list</a>

    <a href="{{ path('film_controller_class_edit', {'id': film.id}) }}">edit</a>

    {{ include('film_controller_class/_delete_form.html.twig') }}
{% endblock %}
